name: CI

on: [push]

jobs:
  report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm install

      - name: Install dependencies
        run: npm run build

      - name: Run custom action
        uses: ./.github/actions/my-custom-action
        with:
          xml-file: "./.github/testng.xml"
